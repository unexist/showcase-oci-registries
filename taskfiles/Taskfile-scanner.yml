version: '3'

tasks:
  trivy:
    cmds:
      - |
        podman run -it --rm \
          -v ./{{.SBOM_FILE}}:/{{.SBOM_FILE}} \
          docker.io/aquasec/trivy:latest sbom /{{.SBOM_FILE}}

  grype:
    cmds:
      - |
        podman run -it --rm \
          -v ./{{.SBOM_FILE}}:/{{.SBOM_FILE}} \
          docker.io/anchore/grype:latest sbom:/{{.SBOM_FILE}}